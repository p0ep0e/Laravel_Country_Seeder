# Laravel_Country_Seeder
A Laravel Seeder including 2 and 3 letter ISO codes and the country TLD, optimised column types.

Note that the ID data type is smallint so if you reseed many times (as its set to autoincremen), eventually you could run out of numbers.
